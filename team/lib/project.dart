class Project {
  String title;
  String duration;
  double progress;
  bool isCompleted;

  Project({
    required this.title,
    required this.duration,
    required this.progress,
    required this.isCompleted,
  });
}
